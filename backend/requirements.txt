# ─────────────────────────────────────────────
# Syllabot Backend — Python Dependencies
# ─────────────────────────────────────────────

# --- Web Framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9

# --- Database ---
sqlalchemy>=2.0.28
alembic>=1.13.1
psycopg2-binary>=2.9.9      # PostgreSQL driver (production)
asyncpg>=0.29.0             # Async PostgreSQL driver

# --- Data Validation ---
pydantic[email]>=2.6.4
pydantic-settings>=2.2.1

# --- Authentication & Security ---
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0

# --- HTTP Client (for legacy LLMService) ---
httpx>=0.27.0

# ─────────────────────────────────────────────
# Agentic AI — LangGraph & Providers
# ─────────────────────────────────────────────

# Core LangGraph orchestration
langgraph>=0.2.0
langchain-core>=0.3.0

# Gemini provider (semantic reasoning, parsing)
langchain-google-genai>=2.0.0

# Groq provider (fast inference, planning, quizzes)
langchain-groq>=0.2.0

# ─────────────────────────────────────────────
# Observability & Security
# ─────────────────────────────────────────────

# Structured JSON logging
python-json-logger>=2.0.7

# API rate limiting
slowapi>=0.1.9

# ─────────────────────────────────────────────
# Testing
# ─────────────────────────────────────────────
pytest>=8.1.1
pytest-asyncio>=0.23.0
